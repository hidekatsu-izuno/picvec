[tools]
python = "3.14.6"
rust = "1.95.0"
uv = "0.12.1"

[tasks."build:wasm"]
description = "Build WebAssembly and update docs/pkg for the browser converter"
dir = "{{config_root}}"
run = "./scripts/build-wasm.sh"
