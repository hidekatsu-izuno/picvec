# 性能劣化の切り分け・巻き戻し（2026-09-14）

ユーザーの指示により調査を中断し、文書化した。劣化前の最終段階は未確定。全変更の記録・保存済み資料・再適用用パッチは [高速化スレッドの記録](performance-thread-2026-09.md) を参照。

- 調査開始時HEAD: `3fa41d2`。バックアップ: `/tmp/picvec-regression-20260914/before.tar`。
- ソース・比較スクリプト・テストを、このスレッド開始時点の `725a082` に復元した。追加された実装ファイルも削除し、元の各ファイルとのバイト一致を確認した。Git履歴は書き換えていない。
- `target/release/picvec` も同版の保存済みバイナリへ復元した。SHA-256: `e7cab41ea5becbfbdca2bb2a880f9f386a04166c150828b52f81e78e079d568d`。過去の測定記録 `docs/performance-2026-09-13.json` の基準バイナリと一致する。
- CPU上限の設定は変更していない。未追跡の `sample/output/conversion-timings.csv` は保持し、バックアップも保存した。

## 計測で判明した問題

`725a082` のbooster-layoutは、通常変換を約32秒で終えた後、5770×2058全体の高精細化に進み、600秒でタイムアウトした。ただし後述の同時実行があったため、時間は正式な比較値として使わない。高精細化の全面再変換は `725a082` で追加され、それ以降の高速化コミットでは変更されていない。

一つ前の `415d64f` は一時ディレクトリに展開して比較用にビルドしたが、同時実行判明後に計測を停止した。この版のソースは作業ディレクトリには適用していない。旧版は透過・画質の変更も含むため、そのまま安全な採用候補とは断定できない。

制限されたプロセス名前空間では既存の実行が見えず、ログの更新を手掛かりにホスト側で確認したところ、cliparts-6x6が2重に実行されていた。それぞれ `--threads 4` を指定している。

1. `scripts/generate_sample_svgs.sh` からの変換。一時出力 `.regenerate-1trDQh/cliparts-6x6.svg`。
2. `/tmp/picvec-timing-run/run.py` からの変換。出力 `sample/output/cliparts-6x6.svg`。

既存処理のログでは、穴の簡略化に1393.238秒かかっていた。こちらも同時実行の影響を含む。

## 承認後の再計測と中断

既存の2変換と親スクリプトの停止は自動承認レビューに拒否された。理由は、進行中の成果や後続画像の処理を失う可能性があり、停止が明示的に承認されていないため。今回開始した一時比較だけは停止した。

その後ユーザーが停止を明示承認した。再確認時には既存の2変換と親スクリプトは終了しており、cliparts-6x6には3246.552秒で完了した記録が残っていた。重複実行下の値なので正式な比較には使わない。

単独で再測定した開始時点のbooster-layoutは、通常変換を約28.55秒で終えた後、全面高精細化に進み、300.065秒でタイムアウトした。最新段階も通常変換を約21.80秒で終え、同じ全面高精細化候補へ進んだが、ユーザーの文書化指示で途中停止した。最新段階の結果を完走やタイムアウトとは扱わない。cliparts-6x6の単独比較は未実施。

文書化への切り替え時に今回の比較用プロセスを停止した。後続段階は再適用していない。主要なログ・段階別バイナリ対応は [保存した調査資料](performance-thread-2026-09/investigation/) に複製済み。元の一時資料は `/tmp/picvec-regression-20260914/` にある。
